// import { redisGetAsync } from "../../services/cache-redis";

export const validateUserIsLoggedIn = async (req, res, next) => {
  // const user = await redisGetAsync('user');
  // if (!user) return false;
  return true;
}